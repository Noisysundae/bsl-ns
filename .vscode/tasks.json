{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Git: Fetch Everything",
			"type": "shell",
			"command": "git fetch --all --tags",
			"presentation": {
				"close": true,
				"reveal": "silent"
			},
			"windows": {
				"options": {
					"cwd": "${workspaceFolder}"
				}
			},
			"problemMatcher": []
		},
	]
}